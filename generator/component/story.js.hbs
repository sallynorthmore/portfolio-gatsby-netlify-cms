import React from 'react';
import { storiesOf } from '@storybook/react';
import {{ properCase name }} from './index';

const stories = storiesOf('{{ properCase name }}', module);

stories.add(
    'default',
    () => {
        return <{{ properCase name }} />;
    });
